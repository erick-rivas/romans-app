import React from "react";
import PropTypes from "prop-types";
import View from "views/authentication/Logout";

function Logout(props) {
  return <View
  />;
}

Logout.propTypes = {};

export default Logout;
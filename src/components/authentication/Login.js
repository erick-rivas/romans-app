import React from "react";
import PropTypes from "prop-types";
import View from "views/authentication/Login";

function Login(props) {
  return <View
  />;
}

Login.propTypes = {};

export default Login;